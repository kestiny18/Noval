import {render,screen} from "@testing-library/react";import {beforeEach,expect,it,vi} from "vitest";import {App} from "./App";
beforeEach(()=>{window.noval={chooseWorkspace:vi.fn().mockResolvedValue(null),getWorkspace:vi.fn().mockResolvedValue(null),listSessions:vi.fn(),createSession:vi.fn(),resumeSession:vi.fn(),renameSession:vi.fn(),transcript:vi.fn(),startTurn:vi.fn(),cancelTurn:vi.fn(),setPermissionMode:vi.fn(),resolvePermission:vi.fn(),onEvent:vi.fn().mockReturnValue(()=>{}),appInfo:vi.fn(),getProviderProfile:vi.fn().mockResolvedValue(null),saveProviderProfile:vi.fn()} as any});
it("requires a workspace before creating a task",async()=>{render(<App/>);expect(await screen.findByRole("button",{name:/choose workspace/i})).toBeVisible();expect(screen.queryByText("New task")).not.toBeInTheDocument()});
