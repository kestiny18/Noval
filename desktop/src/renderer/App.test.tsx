import {cleanup,fireEvent,render,screen,waitFor} from "@testing-library/react";import {afterEach,beforeEach,expect,it,vi} from "vitest";import {App} from "./App";
const session={session_id:"session-1",workdir:"C:/workspace",provider:"test",model:"model",is_open:false,title:"First task",message_count:0};
beforeEach(()=>{window.noval={chooseWorkspace:vi.fn().mockResolvedValue(null),getWorkspace:vi.fn().mockResolvedValue(null),listSessions:vi.fn().mockResolvedValue([]),createSession:vi.fn(),resumeSession:vi.fn(),renameSession:vi.fn(),transcript:vi.fn().mockResolvedValue({entries:[],next_sequence:0,has_more:false}),replayEvents:vi.fn().mockResolvedValue({events:[],next_sequence:0,gap_detected:false,has_more:false}),startTurn:vi.fn(),cancelTurn:vi.fn(),setPermissionMode:vi.fn(),revokeTool:vi.fn(),resetPermissions:vi.fn(),resolvePermission:vi.fn(),onEvent:vi.fn().mockReturnValue(()=>{}),appInfo:vi.fn(),getProviderProfile:vi.fn().mockResolvedValue(null),saveProviderProfile:vi.fn(),exportDiagnostics:vi.fn()} as any});
afterEach(cleanup);
it("requires a workspace before creating a task",async()=>{render(<App/>);expect(await screen.findByRole("button",{name:/choose workspace/i})).toBeVisible();expect(screen.queryByText("New task")).not.toBeInTheDocument()});
it("renames a resumed Session through the narrow host API",async()=>{vi.mocked(window.noval.getWorkspace).mockResolvedValue("C:/workspace");vi.mocked(window.noval.listSessions).mockResolvedValue([session]);vi.mocked(window.noval.resumeSession).mockResolvedValue({session:{...session,is_open:true},permissions:{mode:"ask",approved_tools:[]}});vi.mocked(window.noval.renameSession).mockResolvedValue({...session,is_open:true,title:"Renamed task"});render(<App/>);fireEvent.click(await screen.findByRole("button",{name:/first task/i}));fireEvent.click(await screen.findByRole("button",{name:/rename task/i}));const input=screen.getByRole("textbox",{name:/task title/i});fireEvent.change(input,{target:{value:"Renamed task"}});fireEvent.click(screen.getByRole("button",{name:/save title/i}));await waitFor(()=>expect(window.noval.renameSession).toHaveBeenCalledWith("session-1","Renamed task"));expect((await screen.findAllByText("Renamed task")).length).toBeGreaterThan(0)});
it("shows Core completion evidence separately from the answer",async()=>{vi.mocked(window.noval.getWorkspace).mockResolvedValue("C:/workspace");vi.mocked(window.noval.listSessions).mockResolvedValue([session]);vi.mocked(window.noval.resumeSession).mockResolvedValue({session:{...session,is_open:true},permissions:{mode:"ask",approved_tools:[]}});vi.mocked(window.noval.startTurn).mockResolvedValue({status:"completed",completion:{goal_id:"g1",status:"uncertain",evaluated_at:"2026-07-22T00:00:00Z",criteria:[{criterion_id:"tests",status:"missing",source:null,observed_at:null}]}});render(<App/>);fireEvent.click(await screen.findByRole("button",{name:/first task/i}));const composer=await screen.findByRole("textbox",{name:/message noval/i});fireEvent.change(composer,{target:{value:"Do the work"}});fireEvent.click(screen.getByRole("button",{name:/send/i}));expect(await screen.findByRole("region",{name:/completion evidence/i})).toHaveTextContent("Completion uncertain");expect(screen.getByText("tests")).toBeVisible()});
