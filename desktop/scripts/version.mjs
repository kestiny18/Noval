import {execFileSync} from "node:child_process";import{readFileSync}from"node:fs";import{fileURLToPath}from"node:url";const root=fileURLToPath(new URL("../..",import.meta.url));const release=JSON.parse(readFileSync(fileURLToPath(new URL("../release-version.json",import.meta.url)),"utf8"));if(!/^\d{2}\.\d{3,4}$/.test(release.releasePrefix))throw new Error("releasePrefix must use YY.MDD format");const count=execFileSync("git",["rev-list","--count","HEAD"],{encoding:"utf8",cwd:root}).trim();process.stdout.write(`${release.releasePrefix}.${count}`);
